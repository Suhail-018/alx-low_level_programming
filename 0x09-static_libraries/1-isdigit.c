#include "main.h"
#include <ctype.h>
/**
* _isdigit - digit from 0-9
* @c: char to check
*
* Return: 0 or 1
*/
int _isdigit(int c)
{
if isdigit(c)
{
return (1);
}
else if isdigit(c)
{
return (0);
}
return (0);
}
