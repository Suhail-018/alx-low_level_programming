#include "main.h"
#include <string.h>
/**
* int *_strcmp - This function compares the s1 string to the s2 string
* @s1: compared
* @s2: compared
* Return: 0
*/
int _strcmp(char *s1, char *s2)
{
int result = strcmp(s1, s2);
return (result);
}
