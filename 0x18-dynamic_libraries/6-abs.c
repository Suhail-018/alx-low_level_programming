#include "main.h"
#include <stdio.h>
/**
*  _abs - it is still unknown is it notpossible idont now
* @n:input
* Return: always 0
*/
int _abs(int n)
{
/* implementation */
n = 1;
printf("%d\n", n);
return (0);
}
